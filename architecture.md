# מסמך ארכיטקטורה - מערכת אוטומציה למיון שיעורים

מסמך זה מתעד את מבנה הפרויקט, התיקיות והקבצים עבור המערכת המלאה.

## מבנה התיקיות והקבצים

```
d:\מיון שיעורים
├── architecture.md             # מסמך זה - תיעוד ארכיטקטורת המערכת והקבצים.
├── PROJECT_STATUS.md           # סטטוס פרויקט מרוכז, הישגים ומפת דרכים להמשך.
├── DEPLOYMENT_GUIDE.md         # מדריך פריסה מסודר ומקוצר לשרת הישיבה (Ubuntu Server).
├── SERVER_FOLDER_STRUCTURE.md  # מיפוי מלא של היררכיית התיקיות, הרבנים והספרים בשרת הישיבה.
├── config.py                   # קובץ הגדרות מרכזי (נתיבים, רשימת כל רבני הישיבה, מודלי STT ו-LLM).
├── install.sh                  # סקריפט התקנה מהיר לאובונטו סרבר.
├── setup_windows.bat           # סקריפט התקנה מהיר לווינדוס.
├── requirements.txt            # רשימת התלויות של Python לפרויקט.
├── analyze_benchmark.py        # סקריפט ניתוח דוחות ביצועים ומדדי דיוק על בסיס history.json למבחן 50 השיעורים.
├── setup_smb_mount.sh          # סקריפט התקנה והגדרת עגינה אוטומטית (fstab) לשרת הקבצים של הישיבה (Windows Server).
├── set_admin_password.py       # כלי אינטראקטיבי להגדרת סיסמת מנהל לממשק ה-Web.
├── .smbcredentials.example    # תבנית קובץ אימות מאובטח למשתמש שרת ה-Windows.
├── recorder.service            # קובץ שירות systemd מוכן להפעלה רציפה ב-Ubuntu Server.
├── main.py                     # סקריפט הריצה הראשי (הפעלת שירות האזנה, שרת Web או שניהם יחד).
├── core/                       # מודולי הליבה של המערכת.
│   ├── __init__.py
│   ├── audio_processor.py      # חיתוך ועיבוד אודיו ראשוני באמצעות ffmpeg.
│   ├── transcriber.py          # המרת דיבור עברי לטקסט בדיוק מקסימלי (ivrit-ai / faster-whisper).
│   ├── ai_analyzer.py          # חילוץ ישויות ושמות מתוך התמלול באמצעות Gemma 4 מקומי.
│   ├── hebrew_date.py          # המרת תאריכי קובץ מקוריים לתאריך עברי אותנטי.
│   ├── file_manager.py         # העברה מאובטחת, בדיקת Checksum, ומניעת דריסה.
│   ├── network_share.py        # ניהול עגינת שרת הישיבה (SMB/CIFS), חוסן רשת ומנגנון Staging Offline Buffer.
│   ├── usb_ingest.py           # זיהוי אוטומטי ושאיבה מאובטחת של הקלטות מכונני USB/מקלטים.
│   ├── notes_manager.py        # מודול ניהול פניות והערות תלמידים (CRUD על data/notes.json).
│   └── watcher.py              # שירות רקע המנהל את כל הצינור ומאזין ל-incoming/.
├── web/                        # שרת ה-Web והממשקים הגרפיים (FastAPI + HTML/CSS).
│   ├── server.py               # שרת ה-API ומזרים האודיו ללא הורדה.
│   └── static/                 # קבצי ממשק משתמש (HTML, CSS, JS) לאחראי ולתלמידים.
├── client_tools/               # כלי קצה עבור מחשבי הישיבה (ווינדוס).
│   ├── install.bat             # סקריפט התקנה בלחיצה אחת למחשבי הישיבה.
│   └── install_context_menu.reg # סקריפט Registry להוספת כפתור "השאר הערה" בלחיצה ימנית.
└── data/                       # תיקיות המידע והסימולציה המקומית.
    ├── incoming/               # תיקיית קליטה (מדמה מקלטי שמע).
    ├── sorted/                 # תיקיית יעד מקומית לשיעורים ממוינים (במצב עבודה מקומי).
    ├── needs_review/           # תיקיית סיווג ידני לקבצים ללא פתיח / ללא זיהוי (במצב מקומי).
    ├── local_buffer/           # תיקיית חיץ מקומית (Buffer) לשמירת שיעורים מעובדים לפני אימות בשרת הישיבה.
    ├── notes.json              # מסד נתונים של פניות והערות תלמידים.
    └── history.json            # תיעוד היסטוריית העיבודים, התמלולים והלוגים.
```

## פירוט רכיבי המערכת

1. **Config (`config.py`):**
   מרכז את כל ההגדרות, שמות התיקיות, ומאפייני ה-AI במקום אחד.
2. **Audio Processor (`core/audio_processor.py`):**
   מחלץ 35 שניות ראשונות באופן יעיל ומהיר.
3. **Transcriber (`core/transcriber.py`):**
   משתמש במודל ה-ASR של `ivrit-ai` (`whisper-large-v3-turbo-ct2` ב-`int8`) לתמלול עברית ישיבתית בדיוק מקסימלי.
4. **AI Analyzer (`core/ai_analyzer.py`):**
   שולח את התמלול הנקי ל-Gemma 4 לחילוץ מהיר של שם הרב, נושא השיעור, וקביעת סטטוס הזיהוי (כולל Few-Shot Prompting).
5. **Hebrew Date (`core/hebrew_date.py`):**
   ממיר את תאריך יצירת/שינוי הקובץ המקורי לתאריך עברי (כגון `ו_אלול_תשפו`).
6. **File Manager (`core/file_manager.py`):**
   דואג לשלמות הקבצים (Checksum SHA256), מונע דריסת קבצים בעלי שם זהה, מטמיע תגיות מטא-דאטה ID3 (אמן, כותרת ואלבום עברי), ומנהל העברה בטוחה.
7. **Watcher (`core/watcher.py`):**
   מנהל את זרימת המידע המלאה מקצה לקצה.
8. **Benchmark Analyzer (`analyze_benchmark.py`):**
   מנתח את קובץ הלוגים `data/history.json` לאחר עיבוד סדרות שיעורים (כגון מבחן 50 השיעורים), ומפיק מדדי דיוק, התפלגות רבנים, זמני עיבוד, וטבלאות מעמיקות.
9. **Network Share Manager (`core/network_share.py`):**
   מנהל את החיבור לשרת הקבצים של הישיבה (`\\mdserver\שיעורי שמע\שיעורים למיון`), מבצע בדיקות תקינות כתיבה (Health Check), ומנהל מנגנון Staging מקומי חכם שמונע אובדן שיעורים בזמן ניתוקי רשת ומסנכרן אוטומטית ברגע שהשרת מחובר.
10. **Web Admin Dashboard (`web/server.py` & `web/frontend/admin/`):**
    ממשק ניהול נקי ופרקטי המאפשר לאחראי השיעורים להאזין לקבצים (Streaming) ללא הורדה, ולשנות את שמם ותגיות ה-ID3 שלהם. מוגן בסיסמת מנהל מלאה. הגישה מתבצעת מדפדפן סטנדרטי.
11. **Student Portal & Context Menu (`web/frontend/student/` & `client_tools/install_context_menu.reg`):**
    מנגנון המאפשר לתלמידים להשאיר הערות ישירות מסייר הקבצים בווינדוס (על ידי לחיצה ימנית על קובץ שמע), ללא צורך בהזדהות או התקנת תוכנה.
12. **Password Configuration Utility (`set_admin_password.py`):**
    כלי אינטראקטיבי להגדרה ושינוי מאובטח של סיסמת המנהל לממשק ה-Web (שמירה ב-.env).

> [!NOTE]
> **החלטה ארכיטקטונית - גישה היברידית לממשק משתמש (Web + Windows Integration):**
> לאור העובדה שתלמידים והאחראי רגילים לעבוד בסייר הקבצים של Windows, נבחרה ארכיטקטורה היברידית:
> 1. **חוויית תלמיד "שקופה" (Zero-Friction):** תפריט לחיצה ימנית (דרך Registry) מאפשר לתלמיד לכתוב הערה על שיעור מתוך סייר הקבצים ללא כניסה לאתר, למרות שלתלמיד יש הרשאות "קריאה בלבד" לספריית הרשת (השרת רושם את ההערה).
> 2. **ייעול עבודת מנהל:** מנגד, כדי שהאחראי לא ייאלץ להאזין ולערוך תגיות ID3 מסורבלות דרך Windows, הוא משתמש בלוח בקרה אינטרנטי (Web Dashboard) חכם שמרכז לו את הטיפול בכל השיעורים והערות התלמידים במקום אחד מוגן-סיסמה.
