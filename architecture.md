# מסמך ארכיטקטורה - מערכת שאיבה ומיון שיעורי תורה

מסמך זה מתעד את מבנה הפרויקט, התיקיות, הקבצים וזרימת הנתונים בענף הראשי (`main`).

---

## 🎯 מטרת המערכת
מערכת קלת-משקל (Lightweight Ingestion Engine) הפועלת על גבי שרת מקומי (Ubuntu Server) בישיבה. המערכת מזהה חיבור מקלטי שמע / דיסק-און-קי, שואבת ומאמתת את קבצי השמע באופן מאובטח (SHA-256), מנקה את המקלט כדי שיהיה פנוי לתלמיד הבא, ומעבירה את השיעורים תחת שמות מבוססי תאריך ושעה אותנטיים ישירות לתור המיון (`needs_review` / `שיעורים למיון`).

---

## 📂 מבנה התיקיות והקבצים

```
yeshiva-recorder/
├── architecture.md             # מסמך זה - תיעוד ארכיטקטורת המערכת ומודולי הליבה.
├── PROJECT_STATUS.md           # תמונת מצב הפרויקט, הישגים ומפת דרכים מעודכנת.
├── BRANCHES_OVERVIEW.md        # מפת ענפי הפיתוח ב-Git (כולל ענף ה-AI הנפרד וענף ה-Web).
├── DEPLOYMENT_GUIDE.md         # מדריך פריסה והתקנה נקייה באובונטו סרבר.
├── SERVER_FOLDER_STRUCTURE.md  # מיפוי מלא של היררכיית התיקיות, הרבנים והספרים בשרת הישיבה.
├── config.py                   # קובץ הגדרות מרכזי (נתיבים, הגדרות שאיבת USB, תמיכה בסוגי קבצים).
├── install.sh                  # סקריפט התקנה מהיר לאובונטו סרבר.
├── setup_windows.bat           # סקריפט התקנה מהיר לווינדוס.
├── requirements.txt            # רשימת התלויות הרזות של Python (ללא מודלי AI כבדים).
├── recorder.service            # קובץ שירות systemd להפעלה רציפה ויציבה ברקע בשרת.
├── main.py                     # סקריפט הריצה הראשי (הפעלת שירותי השאיבה וההאזנה).
├── core/                       # מודולי הליבה של המערכת.
│   ├── __init__.py
│   ├── usb_ingest.py           # זיהוי אוטומטי, עגינה (Mount), שאיבה מאובטחת ומחיקה מבוקרת מהמקלט.
│   ├── watcher.py              # שירות האזנה ל-incoming, חילוץ זמני יצירה וניתוב לתור המיון.
│   ├── hebrew_date.py          # המרת תאריכי הקלטה מקוריים לתאריכים עבריים אותנטיים.
│   └── file_manager.py         # העברה בטוחה, בדיקת שלמות SHA-256, מניעת דריסה וכתיבת ID3.
└── data/                       # תיקיות המידע והאחסון.
    ├── incoming/               # תיקיית קליטה ראשונית עבור מקלטי השמע.
    ├── needs_review/           # תור השיעורים למיון (שמות מבוססי תאריך ושעה עבריים).
    ├── sorted/                 # תיקיית שיעורים ממוינים (לאחר סיווג בממשק הניהול).
    └── history.json            # תיעוד היסטוריית השאיבות והעיבודים.
```

---

## ⚙️ פירוט רכיבי המערכת

1. **Config (`config.py`):**
   מרכז את כל הנתיבים, הגדרות שאיבת ה-USB (מחיקה אוטומטית לאחר אימות, מרווחי דגימה) וסיומות קבצי השמע הנתמכות.

2. **USB Ingestion Daemon (`core/usb_ingest.py`):**
   * סורק ומזהה חיבור התקני USB באופן שוטף.
   * מבצע עגינה אוטומטית (Mount) בלינוקס עם תמיכה מלאה בשמות קבצים בעברית (`UTF-8` / `CP862`).
   * מעתיק את כל קבצי השמע ל-`data/incoming/` ומאמת התאמת Checksum SHA-256 מלאה.
   * מוחק את ההקלטות מהמקלט רק לאחר אימות מושלם ומנתק את הכונן (Unmount) בבטחה.

3. **Watcher (`core/watcher.py`):**
   * מאזין לתיקיית `data/incoming/`.
   * בודק שהקובץ סיים להיכתב במלואו (`is_file_ready`).
   * מחלץ את תאריך ושעת ההקלטה המקורית של הקובץ.
   * מייצר שם תקני: `YYYY-MM-DD_HH-MM-SS_(תאריך_עברי).mp3` (לדוגמה: `2026-08-30_23-52-52_(יז_אלול_תשפו).mp3`).
   * מעביר את הקובץ מידית (פחות מחצי שנייה) לתור המיון `data/needs_review/`.

4. **Hebrew Date (`core/hebrew_date.py`):**
   ממיר תאריכי יצירת קובץ גרגוריאניים לתאריך עברי מדויק (כגון `יז_אלול_תשפו`) באמצעות ספריית `pyluach`.

5. **File Manager (`core/file_manager.py`):**
   דואג לשלמות קבצים (אימות גיבוב SHA-256), מניעת דריסה באמצעות מזהים ייחודיים (`_1`, `_2`), הטמעת תגיות ID3 ראשוניות וניהול לוג היסטוריה.
