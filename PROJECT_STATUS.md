# סטטוס פרויקט ומפת דרכים - מערכת מיון שיעורי תורה

מסמך זה מרכז את תמונת המצב המלאה של המערכת, ההישגים הטכנולוגיים שהושגו ונבדקו בשרת הישיבה, ותוכנית העבודה המפורטת להמשך הפיתוח וההטמעה.

---

## 1. מה הושלם בהצלחה (Ingestion, Automation & Hardware)

### א. צינור שאיבה אוטומטי ממקלטי שמע (USB Ingestion) - ✅ נבדק בהצלחה על מקלטי אמת!
* **זיהוי התקנים נשלפים (`core/usb_ingest.py`):** סריקה וזיהוי אוטומטי של חיבור מקלט/DOK בתוך 2 שניות.
* **עגינה אוטומטית בלינוקס:** עגינה (`mount`) עם תמיכה מלאה בשמות קבצים בעברית (`UTF-8`).
* **שאיבה מאובטחת ומחיקה מבוקרת:** העתקת כל קבצי השמע ל-`incoming/`, אימות Checksum SHA-256 לכל קובץ, ומחיקת ההקלטות מהמקלט רק לאחר אימות מושלם כדי לפנות מקום לתלמיד הבא.
* **ניתוק בטוח:** ניתוק הכונן בסיום השאיבה.

### ב. מנוע ניתוב מהיר ותיוג תאריכים אותנטי (Timestamp & Hebrew Date)
* **חילוץ זמני הקלטה:** שליפת שעת ותאריך ההקלטה המקוריים מתוך מאפייני הקובץ.
* **המרה לתאריך עברי:** המרה מדויקת לתאריך עברי (למשל `כ_אלול_תשפו`).
* **מתן שמות אוטומטי:** קבצים מקבלים שם אחיד וברור: `YYYY-MM-DD_HH-MM-SS_(תאריך_עברי).mp3`.
* **עיבוד סופר-מהיר:** תהליך השאיבה וההעברה לוקח פחות משנייה (כ-0.02 עד 0.5 שניות לקובץ), עם צריכת זיכרון מינימלית של 3MB בלבד.

### ג. תשתית פריסה בשרת (DevOps & Deployment)
* **קובץ שירות `systemd` (`recorder.service`):** פועל בהצלחה ברקע בשרת האובונטו (`192.168.1.244`).
* **סביבה נקייה:** הוסרו כל התלויות הכבדות מ-`main`, השרת נקי ומותקן מ-0 בצורה רזה ויציבה.
* **ענף AI ייעודי (`feature/ai-integration`):** כל המחקר, המודלים, הפרומפטים והקוד של ה-AI נשמרו בענף נפרד להמשך פיתוח עתידי.

---

## 2. מפת הדרכים לשלבי ההמשך (Roadmap)

### 🌐 שלב 1: ניתוב ישיר לשרת הישיבה (`\\mdserver\שיעורי שמע\שיעורים למיון`)
* [ ] **הגדרת חיבור שיתוף רשת (Samba/SMB):** חיבור שרת האובונטו לשרת הקבצים המרכזי של הישיבה (`setup_smb_mount.sh`).
* [ ] **ניתוב ישיר ליעד ברשת:** קבצים שישאבו מהמקלטים יועברו ישירות לתיקיית `שיעורים למיון` בשרת הישיבה.

### 🖥️ שלב 2: ממשק ניהול ובקרה (Web Dashboard לאחראי השמע)
* [ ] **תצוגת שיעורים למיון:** הצגת כל הקבצים הממתינים בסיווג עם תאריך ושעה.
* [ ] **נגן שמע משולב בדפדפן:** השמעת השניות הראשונות של ההקלטה כדי לזהות את הרב והנושא.
* [ ] **סיווג מהיר בלחיצת כפתור:** בחירת שם הרב והספר מתוך רשימת הישיבה והעברה אוטומטית לתיקיית הקבע (`אלול התשפ''ו/רבני הישיבה/...`).

### 🔒 שלב 3: אבטחה והזרמת שמע מוגנת לשיעורי הרב צבי (Streaming Only)
* [ ] **ממשק ניווט וחיפוש:** ממשק דמוי סייר קבצים של Windows להאזנה לשיעורי הרב צבי.
* [ ] **חסימת הורדות:** מניעת שמירה ל-DOK והזרמת שמע מוגנת במחשבי הישיבה.
