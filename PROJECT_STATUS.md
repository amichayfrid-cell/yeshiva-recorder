# סטטוס פרויקט ומפת דרכים - מערכת מיון שיעורי תורה

מסמך זה מרכז את תמונת המצב המלאה של המערכת, ההישגים הטכנולוגיים שהושגו עד כה, ותוכנית העבודה המפורטת להמשך הפיתוח וההטמעה בישיבה.

---

## 1. מה הושלם בהצלחה (Backend, AI & Ingestion)

### א. תשתית השמע והתמלול (Audio & STT)
* **חיתוך אודיו מהיר:** חיתוך 35 השניות הראשונות של ההקלטה בלבד באמצעות `ffmpeg` לביצועים מהירים.
* **מודל תמלול ייעודי לעברית:** הטמעת מודל `ivrit-ai/whisper-large-v3-turbo-ct2` (בפורמט `int8` על מעבד CPU) – תמלול מדויק להפליא של עברית ישיבתית.
* **הזרקת הקשר מקדים (Context Biasing):** הזרקת שמות רבני הישיבה ומונחים תורניים ל-`initial_prompt` של Whisper, מה שמונע שגיאות שמיעה כבר בשלב האודיו (למשל תמלול "הרב אורי" ולא "עבורי", "הרב שמריהו" ולא "הרב שמיר הוא").

### ב. מנוע חילוץ ישויות וסיווג (LLM & Prompt Engineering)
* **מודל שפה מקומי:** שימוש ב-Gemma 4 (דרך Ollama) לחילוץ ישויות ללא תלות באינטרנט.
* **הנדסת פרומפטים (Few-Shot Prompting):** פרומפט הכולל דוגמאות מפורשות לזיהוי שמות רבנים ותיקון שגיאות שמיעה מול רשימת רבני הישיבה (`config.KNOWN_RABBIS`).
* **אופטימיזציה ומניעת Timeout:** שליחת 40 המילים הראשונות בלבד ל-LLM, הגבלת אורך פלט (`num_predict: 120`), והורדת טמפרטורה (`0.1`) לתשובה מיידית בתוך 2-3 שניות ללא תקיעות.

### ג. צריבת מטא-דאטה ID3 וניהול קבצים
* **הטמעת תגיות ID3 ב-MP3:** כתיבת שם הרב (`Artist`), נושא השיעור (`Title`) והתאריך העברי (`Album`) ישירות לתוך קובץ השמע באמצעות `mutagen` (לטובת נגנים וטלפונים כשרים).
* **המרת תאריכים עבריים:** המרה מדויקת של תאריך יצירת הקובץ לתאריך עברי אותנטי (למשל `ח_אלול_תשפו`).
* **העברה בטוחה ומניעת דריסה:** בדיקת שלמות SHA-256 ומנגנון ייחודיות שמות.
* **הפרדה ברורה:** קבצים מזוהים עוברים לתיקיות הממוינות, וקבצים ללא פתיח/רב עוברים ל-`data/needs_review/`.
* **רוטציית לוגים אוטומטית:** שמירת 500 הרשומות האחרונות בלבד ב-`history.json`.
* **ביטול שכפול ידני לארכיון:** הודות לשילוב של מערך דיסקים RAID, גיבוי ענן אוטומטי ל-Google Drive, ואימות SHA-256 – בוטל הצורך בהחזקת עותק כפול ידני בשרת, מה שחוסך 50% מנפח האחסון.
* **ניקוי קבצי זמניים:** מחיקת קבצי `tmp*` ישנים בעת הפעלת המערכת.
* **סקריפט איפוס (`reset_data.py`):** מחזיר אוטומטית את כל השיעורים לתיקיית הקליטה עם שמותיהם המקוריים בלחיצה אחת.

### ד. צינור שאיבה אוטומטי ממקלטי שמע (USB Ingestion)
* **זיהוי התקנים נשלפים (`core/usb_ingest.py`):** סריקה וזיהוי אוטומטי של חיבור מקלט/DOK, כולל עגינה אוטומטית בלינוקס (`auto_mount_linux_usb`).
* **שאיבה מאובטחת ומחיקה מבוקרת:** העתקת כל קבצי השמע ל-`incoming/`, אימות Checksum SHA-256, ומחיקת ההקלטות מהמקלט רק לאחר אימות מושלם כדי לפנות מקום לתלמיד הבא.
* **כלי בדיקה וסימולציה (`test_usb_ingest.py`):** כלי CLI לבדיקת שאיבה מספריות, דיסקים או דמו.

### ה. תשתית פריסה לשרת (DevOps & Deployment)
* **קובץ שירות `systemd` (`recorder.service`):** מוכן להפעלה רציפה ברקע של השרת.
* **מדריך פריסה מפורט (`DEPLOYMENT_GUIDE.md`):** הוראות צעד-אחר-צעד להתקנה נקייה באובונטו.
* **מיפוי שרת הישיבה (`SERVER_FOLDER_STRUCTURE.md`):** מיפוי מלא של כל היררכיית התיקיות, הרבנים והספרים מ-`\\mdserver\שיעורי שמע`.

---

## 2. מפת הדרכים לשלבי ההמשך (Roadmap)

### 🎯 שלב 1: מבחן 50 השיעורים וכיול המנוע הקיים (מיידי)
* [x] **מדידת זמנים:** הוספת מדידת משך עיבוד מדויק לכל שיעור ב-`watcher.py` ושמירה ב-`history.json`.
* [x] **כלי ניתוח דוחות ביצועים (`analyze_benchmark.py`):** הפקת דוחות דיוק, זמני עיבוד, התפלגות רבנים וניתוח שגיאות.
* [ ] **הרצת מבחן 50 השיעורים:** הרצת 50 הקלטות אמת מהישיבה על המנוע הקיים.
* [ ] **ניתוח תוצאות וכיול:** שיפור הפרומפט וה-Context Biasing במידת הצורך לפי ממצאי הדוח.

### 🌐 שלב 2: סנכרון מאובטח לשרת הישיבה (הושלם)
* [x] **הגדרת תיקיית יעד אחודה:** הגדרת הנתיב בשרת הישיבה (`שיעורי שמע\שיעורים למיון`).
* [x] **חיבור ושמירה ב-Network Share (Samba/SMB):** אוטומציית חיבור ועגינה לשרת הקבצים המרכזי של הישיבה (`setup_smb_mount.sh`).
* [x] **מנגנון חיץ ואפס אובדן נתונים:** ארכיטקטורת Staging מקומית המונעת אובדן מידע בזמן ניתוקי רשת, ומסנכרנת אוטומטית כשהרשת עולה.
* [x] **שחרור USB מהיר:** שאיבה, אימות ומחיקה מיידית (1-2 שניות) לשחרור המקלט לתלמידים, בעוד העיבוד ממשיך ברקע.

### 💻 שלב 3: ממשק Web היברידי וניהול הערות (בביצוע)
* [ ] **ממשק ניהול (Admin Web Dashboard):** מסך Web נקי, פרקטי ומוגן סיסמה לעריכת תגיות, האזנה (Streaming) ומיון מהיר ללא צורך בהורדת הקבצים.
* [ ] **מנגנון הערות מרכזי (Alerts Inbox):** ריכוז התראות והערות שהושארו על ידי תלמידים (גם על שיעורים שמוינו בעבר).
* [ ] **אינטגרציית Windows לתלמידים:** סקריפט Registry (`.reg`) המוסיף כפתור "השאר הערה" בתפריט הלחיצה הימנית בסייר הקבצים (עבור קובצי שמע בלבד), ללא צורך בסיסמה וללא התקנת תוכנה כבדה.

### 🔮 שלבים עתידיים
* **אבטחה והגנה לשיעורי הרב צבי (Streaming Only):** מניעת הורדה ישירה והזרמת שמע מוגנת במחשבי הישיבה.
